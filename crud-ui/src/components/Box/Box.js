import styled from 'styled-components';

const Box = styled.div`
  border: 0.0625rem solid #d7dfe3;
  background-color: white;
  padding: 1.25rem;
  width: auto;
  box-shadow: 4px 4px 20px 0px rgba(0,0,0,0.25);
`;

export default Box;
